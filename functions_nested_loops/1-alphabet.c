#include "main.h"
/**
 *print_alphabet -print all the alphabet
 *
 * Return: doesn't exist.
 */
void print_alphabet(void)
{
char ch;

for (ch = 'a'; ch <= 'z'; ch++)
{
_putchar(ch);
}
_putchar('\n');
}
