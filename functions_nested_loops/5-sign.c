#include "main.h"
/**
 *print_sign -print sings
 *
 *Return: return 1 0 and -1 depended of n value.
 *
 * @n: variable
 */
int print_sign(int n)
{
	if (n > 0)
		_putchar('+');
	return (1);

		if else(n == 0)
		_putchar('0')
			return (0);

		if (n < 0)
		_putchar('-')
			return (-1);
}
