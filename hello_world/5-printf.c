#include<stdio.h>
#include<string.h>
/**
 * main-starting and ends program
 *
 * Return: 0
 */

int main(void)
{
	char str[] = "with proper grammar, but the outcome is a piece of art,"\n;

	printf("%s", str);

		return (0);
}
